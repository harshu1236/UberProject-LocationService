
spring:
  application:
    name: UberProject-LocationService

  data:
    redis:
          host: localhost
          port: 6379

server:
  port: 7476

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true